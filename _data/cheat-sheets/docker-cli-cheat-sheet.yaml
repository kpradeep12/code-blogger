- column: one
  groups:
    - title: Basic Docker Commands
      items:
      - key: docker pull [OPTIONS] NAME[:TAG\|@DIGEST]
        value: |
          Downloads image  
          -q for less verbose  
          *$docker pull jenkins/jenkins:lts-jdk11*{:.terminal_text}
      - key: |
          docker run [OPTIONS] IMAGE [COMMAND] [ARG...]
        value: |
          Creates a new container
          -d 